# BuildPro Mobile — Environment Variables
# Copy to .env.local and fill in your values

# Your BuildPro backend URL
EXPO_PUBLIC_API_URL=https://api.buildpro.io

# For local development:
# EXPO_PUBLIC_API_URL=http://192.168.1.x:3001
# (use your machine's local IP, not localhost — device can't reach localhost)
